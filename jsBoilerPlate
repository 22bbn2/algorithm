const fs = require('fs')

const getInput = (filePath) => {
    return fs
        .readFileSync(filePath)
        .toString()
        .split(/\n/)
        .map((elem) => (elem.split(" ")));
    }
    
const input = getInput('./examples.txt'); // 제출시 여기 '/dev/stdin' 으로 변경
